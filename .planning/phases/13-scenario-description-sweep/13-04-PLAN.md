---
phase: 13-scenario-description-sweep
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - Transport/Mcp/Tools/ListAssemblyTypesTool.cs
  - Transport/Mcp/Tools/ListNamespaceTypesTool.cs
autonomous: true
requirements:
  - DESC-01
  - DESC-02
gap_closure: true

must_haves:
  truths:
    - "list_assembly_types description uses scenario-oriented language (not NuGet-consumer)"
    - "list_namespace_types description uses scenario-oriented language"
    - "list_assembly_types description cross-references list_namespace_types with scope guidance"
    - "list_namespace_types description cross-references list_assembly_types with scope guidance"
    - "Both assemblyPath parameters use standardized format with (.dll/.exe)"
  artifacts:
    - path: "Transport/Mcp/Tools/ListAssemblyTypesTool.cs"
      provides: "Scenario-oriented description with cross-reference to list_namespace_types"
      contains: "broad inventory"
    - path: "Transport/Mcp/Tools/ListNamespaceTypesTool.cs"
      provides: "Scenario-oriented description with cross-reference to list_assembly_types"
      contains: "list_assembly_types"
  key_links:
    - from: "Transport/Mcp/Tools/ListAssemblyTypesTool.cs"
      to: "list_namespace_types"
      via: "Description attribute text"
      pattern: "list_namespace_types"
    - from: "Transport/Mcp/Tools/ListNamespaceTypesTool.cs"
      to: "list_assembly_types"
      via: "Description attribute text"
      pattern: "list_assembly_types"
---

<objective>
Close the remaining SC #3 gap from Phase 13 verification: restore scenario-oriented descriptions and bidirectional cross-references in ListAssemblyTypesTool.cs and ListNamespaceTypesTool.cs that were regressed by commit 1740974.

Purpose: Complete DESC-01 and DESC-02 requirements by fixing the two tool descriptions that were accidentally reverted to pre-Phase-13 text during the previous gap closure.
Output: Both tools have scenario-oriented descriptions that cross-reference each other with scope guidance.
</objective>

<execution_context>
@C:/Users/cervo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/cervo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-scenario-description-sweep/13-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore scenario descriptions and bidirectional cross-references</name>
  <files>Transport/Mcp/Tools/ListAssemblyTypesTool.cs, Transport/Mcp/Tools/ListNamespaceTypesTool.cs</files>
  <read_first>
    - Transport/Mcp/Tools/ListAssemblyTypesTool.cs (lines 24-29 — current description and parameter attributes)
    - Transport/Mcp/Tools/ListNamespaceTypesTool.cs (lines 27-33 — current description and parameter attributes)
  </read_first>
  <action>
Edit ListAssemblyTypesTool.cs:
- Line 25: Replace the entire [Description] string with exactly:
  "Use this when you need a broad inventory of what an unfamiliar binary contains before narrowing scope. Lists all public types grouped by namespace (names only, no signatures). For a single namespace with full signatures and member counts, use list_namespace_types instead."
- Line 27: Replace the assemblyPath [Description] string with exactly:
  "Path to the .NET assembly (.dll/.exe)"

Edit ListNamespaceTypesTool.cs:
- Line 28: Replace the entire [Description] string with exactly:
  "Use this when you know which namespace to investigate and want a detailed inventory before drilling into individual types. Returns full signatures, member counts, and public method signatures. For a lighter assembly-wide listing by namespace (names only, no signatures), use list_assembly_types instead."
- Line 30: Replace the assemblyPath [Description] string with exactly:
  "Path to the .NET assembly (.dll/.exe)"
  </action>
  <verify>
    <automated>cd C:/Users/cervo/Desktop/Code/ILSpy-MCP && grep -n "broad inventory" Transport/Mcp/Tools/ListAssemblyTypesTool.cs && grep -n "list_namespace_types" Transport/Mcp/Tools/ListAssemblyTypesTool.cs && grep -n "list_assembly_types" Transport/Mcp/Tools/ListNamespaceTypesTool.cs && grep -n "know which namespace" Transport/Mcp/Tools/ListNamespaceTypesTool.cs && grep -c "Path to the .NET assembly (.dll/.exe)" Transport/Mcp/Tools/ListAssemblyTypesTool.cs Transport/Mcp/Tools/ListNamespaceTypesTool.cs && grep -c "NuGet" Transport/Mcp/Tools/ListAssemblyTypesTool.cs | grep "^0$" && dotnet build ILSpy.Mcp.sln --no-restore -v q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - ListAssemblyTypesTool.cs line 25 contains "broad inventory of what an unfamiliar binary contains"
    - ListAssemblyTypesTool.cs line 25 contains "use list_namespace_types instead"
    - ListAssemblyTypesTool.cs line 25 does NOT contain "NuGet"
    - ListAssemblyTypesTool.cs line 27 contains "Path to the .NET assembly (.dll/.exe)"
    - ListNamespaceTypesTool.cs line 28 contains "know which namespace to investigate"
    - ListNamespaceTypesTool.cs line 28 contains "use list_assembly_types instead"
    - ListNamespaceTypesTool.cs line 30 contains "Path to the .NET assembly (.dll/.exe)"
    - dotnet build succeeds with 0 errors
  </acceptance_criteria>
  <done>Both tool descriptions are scenario-oriented with bidirectional cross-references and standardized assemblyPath parameter descriptions. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected. This plan modifies only static [Description] attribute strings — no runtime behavior, input handling, or data flow changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04-01 | N/A | Description attributes | accept | Static string metadata only; no attack surface |
</threat_model>

<verification>
1. grep confirms "broad inventory" in ListAssemblyTypesTool.cs
2. grep confirms "list_namespace_types" in ListAssemblyTypesTool.cs (forward cross-ref)
3. grep confirms "list_assembly_types" in ListNamespaceTypesTool.cs (backward cross-ref)
4. grep confirms zero "NuGet" occurrences in ListAssemblyTypesTool.cs
5. grep confirms "Path to the .NET assembly (.dll/.exe)" in both files
6. dotnet build succeeds
</verification>

<success_criteria>
SC #3 from Phase 13 roadmap is satisfied: list_assembly_types and list_namespace_types descriptions cross-reference each other with guidance on scope difference (whole assembly vs. single namespace). Both descriptions use scenario-oriented "Use this when..." framing per DESC-01.
</success_criteria>

<output>
After completion, create `.planning/phases/13-scenario-description-sweep/13-04-SUMMARY.md`
</output>
