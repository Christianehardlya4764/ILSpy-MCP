---
phase: 13-scenario-description-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Transport/Mcp/Tools/AnalyzeAssemblyTool.cs
  - Transport/Mcp/Tools/DecompileMethodTool.cs
  - Transport/Mcp/Tools/DecompileNamespaceTool.cs
  - Transport/Mcp/Tools/DecompileTypeTool.cs
  - Transport/Mcp/Tools/DisassembleMethodTool.cs
  - Transport/Mcp/Tools/DisassembleTypeTool.cs
  - Transport/Mcp/Tools/ExportProjectTool.cs
  - Transport/Mcp/Tools/ExtractResourceTool.cs
  - Transport/Mcp/Tools/ListAssemblyTypesTool.cs
  - Transport/Mcp/Tools/ListEmbeddedResourcesTool.cs
  - Transport/Mcp/Tools/LoadAssemblyDirectoryTool.cs
  - Transport/Mcp/Tools/ResolveTypeTool.cs
  - Transport/Mcp/Tools/SearchMembersByNameTool.cs
autonomous: true
requirements:
  - DESC-01
  - DESC-02
must_haves:
  truths:
    - "Every tool in this batch has a three-part description: purpose, RE scenario, output hint"
    - "No tool description uses NuGet-library-consumer language like 'NuGet package', 'just installed', 'library'"
    - "Cross-reference pairs within this batch are woven inline with cost/capability guidance"
    - "Parameter descriptions are concise and direct, no over-explaining"
  artifacts:
    - path: "Transport/Mcp/Tools/AnalyzeAssemblyTool.cs"
      provides: "RE-framed tool + parameter descriptions"
      contains: "Use this when"
    - path: "Transport/Mcp/Tools/DecompileTypeTool.cs"
      provides: "RE-framed description with get_type_members cross-reference"
      contains: "get_type_members"
    - path: "Transport/Mcp/Tools/ListAssemblyTypesTool.cs"
      provides: "RE-framed description with decompile_namespace cross-reference"
      contains: "decompile_namespace"
    - path: "Transport/Mcp/Tools/LoadAssemblyDirectoryTool.cs"
      provides: "RE-framed description with resolve_type cross-reference"
      contains: "resolve_type"
    - path: "Transport/Mcp/Tools/DisassembleTypeTool.cs"
      provides: "RE-framed description with disassemble_method cross-reference"
      contains: "disassemble_method"
  key_links:
    - from: "Transport/Mcp/Tools/DecompileTypeTool.cs"
      to: "Transport/Mcp/Tools/GetTypeMembersTool.cs"
      via: "inline cross-reference in description text"
      pattern: "get_type_members"
    - from: "Transport/Mcp/Tools/ListAssemblyTypesTool.cs"
      to: "Transport/Mcp/Tools/DecompileNamespaceTool.cs"
      via: "inline cross-reference in description text"
      pattern: "decompile_namespace"
---

<objective>
Rewrite tool-level and parameter-level [Description] attributes for 13 tools (decompile/disassemble/export/list/load/resolve/extract/search_members_by_name group) to the three-part reverse-engineering-oriented pattern with inline cross-references.

Purpose: Agents reading the tool catalogue should immediately know WHEN to reach for each tool from a reverse-engineering perspective, not just WHAT it does mechanically.
Output: 13 tool files with updated [Description] attributes on both the tool method and all parameters.
</objective>

<execution_context>
@C:/Users/cervo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/cervo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-scenario-description-sweep/13-CONTEXT.md
@.claude/skills/mcp-tool-design/SKILL.md
@.planning/quick/260410-audit-mcp-tools-for-ai-effectiveness-and/260410-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite tool and parameter descriptions for decompile/disassemble/export group (7 tools)</name>
  <files>
    Transport/Mcp/Tools/AnalyzeAssemblyTool.cs
    Transport/Mcp/Tools/DecompileMethodTool.cs
    Transport/Mcp/Tools/DecompileNamespaceTool.cs
    Transport/Mcp/Tools/DecompileTypeTool.cs
    Transport/Mcp/Tools/DisassembleMethodTool.cs
    Transport/Mcp/Tools/DisassembleTypeTool.cs
    Transport/Mcp/Tools/ExportProjectTool.cs
  </files>
  <read_first>
    Transport/Mcp/Tools/AnalyzeAssemblyTool.cs
    Transport/Mcp/Tools/DecompileMethodTool.cs
    Transport/Mcp/Tools/DecompileNamespaceTool.cs
    Transport/Mcp/Tools/DecompileTypeTool.cs
    Transport/Mcp/Tools/DisassembleMethodTool.cs
    Transport/Mcp/Tools/DisassembleTypeTool.cs
    Transport/Mcp/Tools/ExportProjectTool.cs
    .planning/phases/13-scenario-description-sweep/13-CONTEXT.md
  </read_first>
  <action>
Replace the [Description] attribute on the tool method AND all parameter [Description] attributes in each file below. Use the EXACT text specified. The pattern per D-01 is: purpose + RE scenario ("Use this when...") + output hint. Per D-02, all framing is reverse-engineering-centric (analyzing compiled binaries, not consuming NuGet packages). Per D-04, cross-references are woven inline (no separate "See also"). Per D-07, parameter descriptions are concise.

**AnalyzeAssemblyTool.cs** — Tool description:
```
"Summarizes an assembly's architecture: namespaces, key public types, design patterns, and entry points. Use this when starting analysis of an unfamiliar binary to decide which namespaces and types to investigate further. Returns a structured overview with truncation metadata."
```
Parameter descriptions:
- assemblyPath: `"Path to the .NET assembly (.dll/.exe)"`
- query: `"Focus area for analysis (e.g., 'public API surface', 'design patterns', 'entry points')"`

**DecompileTypeTool.cs** — Tool description (cross-refs: get_type_members, disassemble_type per D-05):
```
"Decompiles a type to reconstructed C# source showing full implementation details. Use this when you need to read method bodies, understand control flow, or see how a class actually works inside a compiled binary. For a quick structural overview without source (cheaper), use get_type_members instead; for IL-level representation, use disassemble_type. Returns C# source with truncation metadata."
```
Parameter descriptions:
- assemblyPath: `"Path to the .NET assembly (.dll/.exe)"`
- typeName: `"Full type name (e.g., 'System.String', 'MyApp.Services.OrderProcessor')"`
- query: `"Focus area (e.g., 'error handling', 'state mutations', 'dependency usage')"`

**DecompileMethodTool.cs** — Tool description (cross-ref: disassemble_method per D-05):
```
"Decompiles a single method to reconstructed C# source. Use this when you need to understand a method's logic, parameters, and side effects in a compiled binary. For IL-level analysis (compiler tricks, async state machines), use disassemble_method instead. Returns C# source with truncation metadata."
```
Parameter descriptions:
- assemblyPath: `"Path to the .NET assembly (.dll/.exe)"`
- typeName: `"Full name of the type containing the method"`
- methodName: `"Method name to decompile"`
- query: `"Focus area (e.g., 'algorithm logic', 'error handling', 'performance')"`

**DecompileNamespaceTool.cs** — Tool description (cross-ref: list_assembly_types per D-05):
```
"Lists all types in a namespace with full signatures, member counts, and public method signatures. Use this when you know which namespace to investigate and want a detailed inventory before drilling into individual types. For a lighter assembly-wide listing by namespace (names only, no signatures), use list_assembly_types instead. Returns paginated type summaries."
```
Parameter descriptions:
- assemblyPath: `"Path to the .NET assembly (.dll/.exe)"`
- namespaceName: `"Full namespace (e.g., 'System.Collections.Generic')"`
- maxTypes: `"Maximum types to return (default 200)"`

**DisassembleTypeTool.cs** — Tool description (cross-refs: disassemble_method, decompile_type per D-05):
```
"Disassembles a type to CIL/IL showing method signatures, fields, properties, and events in IL format. Use this when you need the raw IL structure of a type -- metadata layout, field offsets, or interface implementation table. For individual method IL bodies, use disassemble_method; for reconstructed C# source, use decompile_type. Returns IL text with truncation metadata."
```
Parameter descriptions:
- assemblyPath: `"Path to the .NET assembly (.dll/.exe)"`
- typeName: `"Full type name (e.g., 'System.String')"`
- showTokens: `"Include raw metadata token numbers (e.g., /* 06000001 */)"`
- resolveDeep: `"Expand generics and show full type signatures for all operand references"`

**DisassembleMethodTool.cs** — Tool description (cross-refs: disassemble_type, decompile_method per D-05):
```
"Disassembles a single method to CIL/IL instruction listing with resolved operands. Use this when C# decompilation is misleading (compiler tricks, async state machines, generic specialization) and you need ground-truth IL behavior. For type-level IL structure, use disassemble_type; for reconstructed C# source, use decompile_method. Returns IL text with truncation metadata."
```
Parameter descriptions:
- assemblyPath: `"Path to the .NET assembly (.dll/.exe)"`
- typeName: `"Full name of the type containing the method"`
- methodName: `"Method name to disassemble"`
- showBytes: `"Include raw opcode byte sequences"`
- showTokens: `"Include raw metadata token numbers"`
- resolveDeep: `"Expand generics and show full type signatures for all operand references"`

**ExportProjectTool.cs** — Tool description:
```
"Exports a .NET assembly as a complete C# project (.csproj + .cs files) to disk. Use this when you need to browse or search decompiled source across an entire assembly outside the MCP tool interface. The target directory must be empty or non-existent. Returns file listing with truncation metadata."
```
Parameter descriptions:
- assemblyPath: `"Path to the .NET assembly (.dll/.exe)"`
- outputDirectory: `"Target directory (must be empty or non-existent)"`
- namespaceFilter: `"Limit export to types in this namespace"`
- maxTypes: `"Maximum types to export (default 500)"`
  </action>
  <verify>
    <automated>cd C:/Users/cervo/Desktop/Code/ILSpy-MCP &amp;&amp; grep -c "Use this when\|Use this to" Transport/Mcp/Tools/AnalyzeAssemblyTool.cs Transport/Mcp/Tools/DecompileMethodTool.cs Transport/Mcp/Tools/DecompileNamespaceTool.cs Transport/Mcp/Tools/DecompileTypeTool.cs Transport/Mcp/Tools/DisassembleMethodTool.cs Transport/Mcp/Tools/DisassembleTypeTool.cs Transport/Mcp/Tools/ExportProjectTool.cs</automated>
  </verify>
  <acceptance_criteria>
    - AnalyzeAssemblyTool.cs contains "starting analysis of an unfamiliar binary"
    - DecompileTypeTool.cs contains "get_type_members" and "disassemble_type" (cross-references per D-05)
    - DecompileMethodTool.cs contains "disassemble_method" (cross-reference per D-05)
    - DecompileNamespaceTool.cs contains "list_assembly_types" (cross-reference per D-05)
    - DisassembleTypeTool.cs contains "disassemble_method" and "decompile_type" (cross-references per D-05)
    - DisassembleMethodTool.cs contains "disassemble_type" and "decompile_method" (cross-references per D-05)
    - No file contains "NuGet" or "just installed" or "library type" in any Description attribute
    - All assemblyPath parameters use "Path to the .NET assembly (.dll/.exe)"
    - ExportProjectTool.cs contains "Use this when"
  </acceptance_criteria>
  <done>All 7 tool files have three-part RE-framed descriptions on both tool methods and parameters, with inline cross-references where applicable. No NuGet-consumer language remains.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite tool and parameter descriptions for list/load/resolve/extract/search_members group (6 tools)</name>
  <files>
    Transport/Mcp/Tools/ExtractResourceTool.cs
    Transport/Mcp/Tools/ListAssemblyTypesTool.cs
    Transport/Mcp/Tools/ListEmbeddedResourcesTool.cs
    Transport/Mcp/Tools/LoadAssemblyDirectoryTool.cs
    Transport/Mcp/Tools/ResolveTypeTool.cs
    Transport/Mcp/Tools/SearchMembersByNameTool.cs
  </files>
  <read_first>
    Transport/Mcp/Tools/ExtractResourceTool.cs
    Transport/Mcp/Tools/ListAssemblyTypesTool.cs
    Transport/Mcp/Tools/ListEmbeddedResourcesTool.cs
    Transport/Mcp/Tools/LoadAssemblyDirectoryTool.cs
    Transport/Mcp/Tools/ResolveTypeTool.cs
    Transport/Mcp/Tools/SearchMembersByNameTool.cs
  </read_first>
  <action>
Replace the [Description] attribute on the tool method AND all parameter [Description] attributes in each file below. Use the EXACT text specified.

**ListAssemblyTypesTool.cs** — Tool description (cross-ref: decompile_namespace per D-05):
```
"Lists all public types in an assembly grouped by namespace, showing type names and kind (class/struct/interface/enum). Use this when you need a broad inventory of what an unfamiliar binary contains before narrowing scope. For a single namespace with full signatures and member counts, use decompile_namespace instead. Returns paginated type listing."
```
Parameter descriptions:
- assemblyPath: `"Path to the .NET assembly (.dll/.exe)"`
- namespaceFilter: `"Filter types to this namespace prefix (case-insensitive)"`

**ListEmbeddedResourcesTool.cs** — Tool description:
```
"Lists all embedded resources in an assembly with name, type, size, and visibility. Use this when investigating configuration files, localization data, or embedded assets baked into a compiled binary. Use extract_resource to retrieve individual resource contents. Returns paginated resource listing."
```
Parameter descriptions:
- assemblyPath: `"Path to the .NET assembly (.dll/.exe)"`

**ExtractResourceTool.cs** — Tool description:
```
"Extracts the content of a named embedded resource. Use this when you need to read configuration, localization tables, or other data embedded in a compiled assembly. Text resources return inline; binary resources return as base64. Supports offset/limit for paginated binary extraction."
```
Parameter descriptions:
- assemblyPath: `"Path to the .NET assembly (.dll/.exe)"`
- resourceName: `"Embedded resource name (e.g., 'MyApp.Resources.config.json')"`
- offset: `"Byte offset for paginated binary extraction"`
- limit: `"Maximum bytes to return for paginated binary extraction"`

**LoadAssemblyDirectoryTool.cs** — Tool description (cross-ref: resolve_type per D-05):
```
"Scans a directory for .NET assemblies and loads them for analysis. Use this when starting work on a multi-assembly project or application directory -- load the directory first, then use resolve_type to find which assembly defines a specific type. Reports loaded assemblies with name/version and skipped files with reasons."
```
Parameter descriptions:
- directoryPath: `"Path to directory containing .NET assemblies"`
- maxDepth: `"Maximum recursion depth for subdirectories (default 3)"`

**ResolveTypeTool.cs** — Tool description (cross-ref: load_assembly_directory per D-05):
```
"Finds which assembly in a directory defines a given type. Use this when you have a type name from decompiled source or IL output and need to locate the assembly that contains it. Supports partial matching (e.g., 'HttpClient' matches 'System.Net.Http.HttpClient'). Use load_assembly_directory first to scan the directory."
```
Parameter descriptions:
- directoryPath: `"Path to directory containing .NET assemblies"`
- typeName: `"Type name to search for (partial match supported, e.g., 'HttpClient')"`
- maxDepth: `"Maximum recursion depth for subdirectories (default 3)"`

**SearchMembersByNameTool.cs** — Tool description:
```
"Searches all types in an assembly for members matching a name pattern. Use this when you know the operation you need (e.g., 'Parse', 'Encrypt', 'Validate') but not which type implements it. Returns matching methods, properties, fields, and events with their declaring type and signature."
```
Parameter descriptions:
- assemblyPath: `"Path to the .NET assembly (.dll/.exe)"`
- searchTerm: `"Name or partial name to match (case-insensitive)"`
- memberKind: `"Filter by kind: method, property, field, or event"`
  </action>
  <verify>
    <automated>cd C:/Users/cervo/Desktop/Code/ILSpy-MCP &amp;&amp; grep -c "Use this when\|Use this to" Transport/Mcp/Tools/ExtractResourceTool.cs Transport/Mcp/Tools/ListAssemblyTypesTool.cs Transport/Mcp/Tools/ListEmbeddedResourcesTool.cs Transport/Mcp/Tools/LoadAssemblyDirectoryTool.cs Transport/Mcp/Tools/ResolveTypeTool.cs Transport/Mcp/Tools/SearchMembersByNameTool.cs</automated>
  </verify>
  <acceptance_criteria>
    - ListAssemblyTypesTool.cs contains "decompile_namespace" (cross-reference per D-05)
    - LoadAssemblyDirectoryTool.cs contains "resolve_type" (cross-reference per D-05)
    - ResolveTypeTool.cs contains "load_assembly_directory" (cross-reference per D-05)
    - No file contains "NuGet" or "just installed" or "added a NuGet package" in any Description attribute
    - All assemblyPath parameters use "Path to the .NET assembly (.dll/.exe)"
    - All directoryPath parameters use "Path to directory containing .NET assemblies"
    - Each tool file contains at least one "Use this when" or "Use this to" in its Description
    - ExtractResourceTool.cs contains "Use this when"
    - ListEmbeddedResourcesTool.cs contains "extract_resource"
    - SearchMembersByNameTool.cs contains "Use this when"
  </acceptance_criteria>
  <done>All 6 tool files have three-part RE-framed descriptions on both tool methods and parameters, with inline cross-references where applicable. No NuGet-consumer language remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan modifies only string literal [Description] attributes -- no logic, no input handling, no output changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | T (Tampering) | Description strings | accept | Description text is static metadata with no runtime effect on security. No user input flows through descriptions. |
</threat_model>

<verification>
1. Every tool file in this batch contains "Use this when" or "Use this to" in its tool-level [Description]
2. No tool file contains "NuGet" or "just installed" in any [Description]
3. Cross-reference pairs per D-05 are present: decompile_type mentions get_type_members, list_assembly_types mentions decompile_namespace, load_assembly_directory mentions resolve_type, disassemble_type mentions disassemble_method, decompile_type mentions disassemble_type, decompile_method mentions disassemble_method
4. `dotnet build ILSpy.Mcp.sln` compiles successfully (descriptions are compile-time strings)
</verification>

<success_criteria>
- All 13 tool files have three-part reverse-engineering-oriented descriptions
- All parameter descriptions are concise and direct
- All D-05 cross-reference pairs that touch files in this plan are woven inline
- Solution compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/13-scenario-description-sweep/13-01-SUMMARY.md`
</output>
