---
phase: 14-v1.2.0-gap-closure-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Transport/Mcp/Tools/AnalyzeReferencesTool.cs
  - Tests/Tools/AnalyzeReferencesToolTests.cs
  - Program.cs
  - docs/PAGINATION.md
  - README.md
autonomous: true
requirements:
  - CLEAN-01
  - PAGE-01
  - CLEAN-03
gap_closure: true

must_haves:
  truths:
    - "AnalyzeReferencesTool source file no longer exists"
    - "AnalyzeReferencesTool DI registration removed from Program.cs"
    - "AnalyzeReferencesToolTests test file no longer exists"
    - "Runtime MCP surface = 27 tools (one [McpServerToolType] removed)"
    - "docs/PAGINATION.md exists in working tree"
    - "docs/PAGINATION.md documents (maxResults, offset) parameters and [pagination:{total,returned,offset,truncated,nextOffset}] footer"
    - "README.md line ~287 link to docs/PAGINATION.md resolves to a real file"
    - "dotnet build succeeds with 0 errors"
    - "dotnet test succeeds with 0 failures"
  artifacts:
    - path: "docs/PAGINATION.md"
      provides: "Canonical pagination contract spec"
      contains: "[pagination:"
    - path: "Program.cs"
      provides: "DI registration without AnalyzeReferencesTool"
      pattern_absent: "AnalyzeReferencesTool"
  key_links:
    - from: "README.md"
      to: "docs/PAGINATION.md"
      via: "markdown link"
      pattern: "docs/PAGINATION.md"
    - from: "PaginationEnvelope.AppendFooter"
      to: "docs/PAGINATION.md"
      via: "doc references same field-order contract"
      pattern: "total.*returned.*offset.*truncated.*nextOffset"
---

<objective>
Close CLEAN-01 (delete the AnalyzeReferencesTool dispatcher, restoring the 27-tool MCP surface) AND PAGE-01/CLEAN-03 (recreate the missing docs/PAGINATION.md so the README link at line ~287 resolves). These two gaps are bundled because they share zero file overlap with the other 14-* plans and both are pure cleanup/documentation work that unblocks downstream verification.

Purpose: The runtime MCP surface currently exposes 28 tools (analyze_references is still registered) while the README advertises 27; docs/PAGINATION.md is missing despite README linking to it. Both are "broken contract" gaps the audit flagged — fixing them closes CLEAN-01 and unblocks the canonical-footer plans (14-02..05) by giving them an authoritative spec to point at.
Output: AnalyzeReferencesTool gone, Program.cs registration gone, tests gone, docs/PAGINATION.md created, README link verified.
</objective>

<execution_context>
@C:/Users/cervo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/cervo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.2.0-MILESTONE-AUDIT.md
@Application/Pagination/PaginationEnvelope.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete AnalyzeReferencesTool, its tests, and remove DI registration</name>
  <files>Transport/Mcp/Tools/AnalyzeReferencesTool.cs, Tests/Tools/AnalyzeReferencesToolTests.cs, Program.cs</files>
  <read_first>
    - Transport/Mcp/Tools/AnalyzeReferencesTool.cs (full file — confirm scope before deletion)
    - Program.cs (line 195 — `services.AddScoped<AnalyzeReferencesTool>();`)
    - Tests/Tools/AnalyzeReferencesToolTests.cs (full file — confirm it tests only the dispatcher)
    - .planning/v1.2.0-MILESTONE-AUDIT.md (CLEAN-01 entry — gap description)
  </read_first>
  <action>
1. Delete the file `Transport/Mcp/Tools/AnalyzeReferencesTool.cs` entirely.
2. Delete the file `Tests/Tools/AnalyzeReferencesToolTests.cs` entirely.
3. Edit `Program.cs`: remove the line `services.AddScoped<AnalyzeReferencesTool>();` (currently at line 195). Do NOT remove any other registration. Also confirm the `using ILSpy.Mcp.Transport.Mcp.Tools;` directive (line 7) stays — it is needed by the other tool registrations.
4. Search for any lingering references: `grep -rn "AnalyzeReferencesTool\|analyze_references" Transport/ Application/ Tests/ Program.cs` — every match must be in a comment, deleted file, or this plan's frontmatter; if any production code or active test still references the type, remove that reference too.
5. Run `dotnet build ILSpy.Mcp.sln` and `dotnet test ILSpy.Mcp.sln` — both must succeed.

Do NOT modify any other tool files. Do NOT add a deprecation shim — per audit + Phase 9 decision, this is a hard delete.
  </action>
  <verify>
    <automated>cd C:/Users/cervo/Desktop/Code/ILSpy-MCP && [ ! -f Transport/Mcp/Tools/AnalyzeReferencesTool.cs ] && [ ! -f Tests/Tools/AnalyzeReferencesToolTests.cs ] && ! grep -rn "AnalyzeReferencesTool" Transport/ Application/ Tests/ Program.cs && ! grep -rn "analyze_references" Transport/ Application/ Tests/ Program.cs && dotnet build ILSpy.Mcp.sln --no-restore -v q 2>&1 | tail -3 && dotnet test ILSpy.Mcp.sln --no-build --no-restore -v q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `Transport/Mcp/Tools/AnalyzeReferencesTool.cs` does not exist
    - `Tests/Tools/AnalyzeReferencesToolTests.cs` does not exist
    - `grep -rn "AnalyzeReferencesTool" Transport/ Application/ Tests/ Program.cs` returns no matches
    - `grep -rn "analyze_references" Transport/ Application/ Tests/ Program.cs` returns no matches
    - `grep -c "\[McpServerToolType\]" Transport/Mcp/Tools/*.cs` returns exactly 27 lines summing to 27 (one per remaining tool file)
    - `dotnet build ILSpy.Mcp.sln` exits 0
    - `dotnet test ILSpy.Mcp.sln` exits 0 with no failures
  </acceptance_criteria>
  <done>AnalyzeReferencesTool and its tests are deleted, Program.cs no longer registers it, build and tests pass, MCP tool surface = 27.</done>
</task>

<task type="auto">
  <name>Task 2: Create docs/PAGINATION.md canonical contract spec</name>
  <files>docs/PAGINATION.md</files>
  <read_first>
    - Application/Pagination/PaginationEnvelope.cs (full file — this is the source of truth for field order: total, returned, offset, truncated, nextOffset)
    - Application/UseCases/FindUsagesUseCase.cs lines 79-112 (canonical reference implementation showing FormatResults + AppendFooter)
    - Transport/Mcp/Tools/FindUsagesTool.cs lines 33-50 (canonical maxResults/offset parameter declarations + bounds validation)
    - README.md lines 283-290 (the link block referencing docs/PAGINATION.md — confirm exact link text we must preserve)
    - .planning/v1.2.0-MILESTONE-AUDIT.md (PAGE-01, CLEAN-03 entries — confirms doc must exist + match working contract)
  </read_first>
  <action>
Create `docs/PAGINATION.md` with the following sections (use exact heading text — downstream plans grep for these section names):

```markdown
# Pagination Contract

Every list-returning, search-returning, or bounded-output tool in this MCP server
follows the same pagination contract. Agents do not need to specify pagination
parameters unless they intend to page — sensible defaults apply and the response
always includes a trailing `[pagination:{...}]` footer so truncation is visible.

## Parameters

Every paginable tool accepts the following two optional parameters:

| Parameter | Type | Default | Purpose |
|-----------|------|---------|---------|
| `maxResults` | int | 100 | Maximum results to return in one call. Hard ceiling: 500. |
| `offset` | int | 0 | Number of results to skip (for paging through truncated results). |

Tools MUST validate `maxResults <= 500` and `maxResults >= 1`, returning
`INVALID_PARAMETER` on violation. `offset < 0` should be normalized to 0 or
rejected with `INVALID_PARAMETER`.

## Footer Format

Every paginable response ends with a single line:

    [pagination:{"total":N,"returned":M,"offset":O,"truncated":T,"nextOffset":X}]

Field order is LOCKED to: `total, returned, offset, truncated, nextOffset`.

| Field | Type | Meaning |
|-------|------|---------|
| `total` | int | Total matches available across all pages. |
| `returned` | int | Number of matches in this response. |
| `offset` | int | Offset used for this call (echoes the input). |
| `truncated` | bool | True if `offset + returned < total` (more pages exist). |
| `nextOffset` | int \| null | If `truncated`, the offset to use on the next call; otherwise `null`. |

## Worked Example

Calling `find_usages` against a member with 250 total call sites, default
`maxResults=100`, `offset=0`:

```
Usages of MyApp.Service.Process: 250 found (showing 1-100)

  [Call] MyApp.Caller.A.RunOnce (IL_0012)
  [Call] MyApp.Caller.A.RunTwice (IL_0034)
  ...
[pagination:{"total":250,"returned":100,"offset":0,"truncated":true,"nextOffset":100}]
```

To get the next page, call again with `offset=100`. The third page
(`offset=200`) returns the final 50 results with `truncated=false` and
`nextOffset=null`.

## Truncation Footer (Source / Bounded-Output Tools)

Source-returning tools (`decompile_type`, `decompile_method`, `disassemble_type`,
`disassemble_method`) and bounded-output tools (`export_project`,
`analyze_assembly`) emit the SAME `[pagination:{...}]` footer when their output
exceeds the configured cap (`MaxDecompilationSize`, default 1 MB), reusing the
fields with this semantic:

| Field | Source/Bounded-Output Semantic |
|-------|--------------------------------|
| `total` | Total bytes (or total types for `export_project`) of the full output. |
| `returned` | Bytes (or types) actually included in this response. |
| `offset` | Always 0 (these tools don't support offset paging). |
| `truncated` | True if the output was capped. |
| `nextOffset` | Always `null` (re-call with narrower scope, not a higher offset). |

This deliberately reuses the canonical envelope so agents have ONE truncation
detection rule across all tools — never search for free-form
"[Output truncated at N bytes...]" strings.

## Implementation

The footer is emitted by the shared helper
`Application.Pagination.PaginationEnvelope.AppendFooter(StringBuilder, int total, int returned, int offset)`.

Every paginable use case MUST call this helper rather than inlining a footer
block. This guarantees field order, JSON shape, and `truncated`/`nextOffset`
computation stay uniform.

## Reference Implementation

`Application/UseCases/FindUsagesUseCase.cs` — `FormatResults` method — is the
canonical reference for new paginable tools. New tools should mirror its
structure: header line with `(showing N-M)` range, body lines, single
`PaginationEnvelope.AppendFooter` call.
```

Do not deviate from the field order or default values. The numbers in the worked
example must satisfy `truncated = (offset + returned < total)` and
`nextOffset = truncated ? offset + returned : null`.

After writing the file, verify the README link at line ~287 resolves: `[ -f docs/PAGINATION.md ]` and the README's literal link text `docs/PAGINATION.md` matches the path. Do NOT modify README.md unless the existing link text is wrong (it currently reads `[\`docs/PAGINATION.md\`](docs/PAGINATION.md)` — leave it alone).
  </action>
  <verify>
    <automated>cd C:/Users/cervo/Desktop/Code/ILSpy-MCP && [ -f docs/PAGINATION.md ] && grep -q "## Footer Format" docs/PAGINATION.md && grep -q "## Parameters" docs/PAGINATION.md && grep -q "## Worked Example" docs/PAGINATION.md && grep -q "## Truncation Footer" docs/PAGINATION.md && grep -q "PaginationEnvelope.AppendFooter" docs/PAGINATION.md && grep -q "total.*returned.*offset.*truncated.*nextOffset" docs/PAGINATION.md && grep -q "docs/PAGINATION.md" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/PAGINATION.md` exists in the working tree
    - `grep "## Footer Format" docs/PAGINATION.md` returns 1 match
    - `grep "## Parameters" docs/PAGINATION.md` returns 1 match
    - `grep "## Worked Example" docs/PAGINATION.md` returns 1 match
    - `grep "## Truncation Footer" docs/PAGINATION.md` returns 1 match
    - `grep "PaginationEnvelope.AppendFooter" docs/PAGINATION.md` returns at least 1 match
    - `grep "\[pagination:" docs/PAGINATION.md` returns at least 2 matches (worked example + format spec)
    - The exact field order `total, returned, offset, truncated, nextOffset` appears together at least once (verifies LOCKED ordering)
    - `grep "docs/PAGINATION.md" README.md` returns at least 1 match (link target unchanged)
  </acceptance_criteria>
  <done>docs/PAGINATION.md exists, documents the canonical contract matching PaginationEnvelope.cs, and the README link resolves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. Task 1 deletes a dispatcher tool that already accepted untrusted MCP input — net reduction in attack surface. Task 2 creates a static markdown doc, no runtime impact.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | Tampering | DI registration removal | accept | Hard-delete is intentional per CLEAN-01; Phase 9 already established no-shim pattern. |
| T-14-01-02 | Information Disclosure | docs/PAGINATION.md | accept | Public protocol contract, no secrets. |
</threat_model>

<verification>
1. AnalyzeReferencesTool.cs and its test file deleted; no lingering grep references in Transport/ Application/ Tests/ Program.cs
2. Program.cs no longer registers AnalyzeReferencesTool; build still succeeds
3. Test suite passes with 0 failures
4. docs/PAGINATION.md exists with required section headings and matches PaginationEnvelope field order
5. README link target docs/PAGINATION.md resolves
</verification>

<success_criteria>
ROADMAP SC #1 (CLEAN-01: AnalyzeReferencesTool.cs gone, no longer registered, runtime MCP surface = 27) and ROADMAP SC #2 (PAGE-01, CLEAN-03: docs/PAGINATION.md exists and matches canonical contract, README link resolves) are satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/14-v1.2.0-gap-closure-sweep/14-01-SUMMARY.md`
</output>
