---
phase: 08-tech-debt-cleanup
plan: 03
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - .planning/milestones/v1.0-phases/07-bulk-operations-documentation/07-01-SUMMARY.md
  - .planning/milestones/v1.0-phases/07-bulk-operations-documentation/07-02-SUMMARY.md
  - .planning/milestones/v1.0-phases/07-bulk-operations-documentation/07-03-SUMMARY.md
autonomous: true
requirements:
  - DEBT-04

must_haves:
  truths:
    - "The full dotnet test suite runs green after all Phase 8 code changes are in place"
    - "Phase 7 test classes (DecompileNamespaceToolTests and ExportProjectToolTests) appear in the run summary with zero failures"
    - "Each Phase 7 SUMMARY file has a '## Runtime Verification' markdown section with date, command, and pass/fail count"
    - "The evidence block is in the markdown BODY, not inside the YAML frontmatter"
    - "Phase 7's 'Self-Check: PASSED (by code inspection only)' caveat is now closed by actual runtime evidence"
  artifacts:
    - path: ".planning/milestones/v1.0-phases/07-bulk-operations-documentation/07-01-SUMMARY.md"
      provides: "Runtime Verification block for DecompileNamespaceToolTests (6 tests)"
      contains: "## Runtime Verification"
    - path: ".planning/milestones/v1.0-phases/07-bulk-operations-documentation/07-02-SUMMARY.md"
      provides: "Runtime Verification block for ExportProjectToolTests (5 tests)"
      contains: "## Runtime Verification"
    - path: ".planning/milestones/v1.0-phases/07-bulk-operations-documentation/07-03-SUMMARY.md"
      provides: "Runtime Verification block (N/A — documentation-only plan, for symmetry)"
      contains: "## Runtime Verification"
  key_links:
    - from: "Phase 7 SUMMARY files"
      to: "Actual dotnet test run output"
      via: "## Runtime Verification markdown section with date, command, and pass/fail counts"
      pattern: "^## Runtime Verification"
---

<objective>
Close DEBT-04 by running the full `dotnet test ILSpy.Mcp.sln` suite and recording the evidence in the three Phase 7 SUMMARY files. This is the phase-gate validation run — it simultaneously verifies: (1) Phase 7's tool tests actually pass at runtime (not just by code inspection), (2) Plan 01's DEBT-01/02 changes did not regress anything, and (3) Plan 02's DEBT-03 doc-only backfill is complete (indirect — a green test run with no code churn confirms Plan 02 is inert on the compile/runtime surface).

Purpose: Replace the Phase 7 "verified by code inspection only" hedge with a captured runtime artifact. The Phase 7 SUMMARY files currently contain `## Self-Check: PASSED` blocks that explicitly note the dotnet CLI was unavailable at Phase 7 execution time. That blocker is gone (dotnet 10.0.201 verified available). This plan closes the loop.

Output: Three Phase 7 SUMMARY files with appended `## Runtime Verification` markdown sections capturing date, command, and pass/fail counts from the actual test run.

**Ordering rationale**: This plan is Wave 2 (depends on Plan 01) because the full-suite run has meaning only AFTER the DEBT-01/02 changes exist — running the suite before Plan 01 would verify the pre-Phase-8 baseline, which is less useful. Plan 02 is Wave 1 parallel (doc-only, zero code churn) and does not affect this plan's ordering.
</objective>

<execution_context>
@C:/Users/cervo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/cervo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-tech-debt-cleanup/08-CONTEXT.md
@.planning/phases/08-tech-debt-cleanup/08-RESEARCH.md
@.planning/phases/08-tech-debt-cleanup/08-VALIDATION.md

# The three Phase 7 SUMMARY files this plan edits
@.planning/milestones/v1.0-phases/07-bulk-operations-documentation/07-01-SUMMARY.md
@.planning/milestones/v1.0-phases/07-bulk-operations-documentation/07-02-SUMMARY.md
@.planning/milestones/v1.0-phases/07-bulk-operations-documentation/07-03-SUMMARY.md

# Plan 01's summary (read for the test count the executor captured)
@.planning/phases/08-tech-debt-cleanup/08-01-SUMMARY.md

<phase_7_test_inventory>
Phase 7 test inventory (verified 2026-04-09 in RESEARCH.md):

| Phase 7 plan | Test file | Test count | Test class |
|--------------|-----------|------------|------------|
| 07-01 (decompile_namespace) | Tests/Tools/DecompileNamespaceToolTests.cs | 6 | DecompileNamespaceToolTests |
| 07-02 (export_project) | Tests/Tools/ExportProjectToolTests.cs | 5 | ExportProjectToolTests |
| 07-03 (documentation) | (none — README.md only) | 0 | — |

DecompileNamespaceToolTests methods: ListsTypesInNamespace, OrdersByKindThenAlphabetically, NestedTypesIndentedUnderParent, InvalidNamespace_ThrowsNamespaceNotFound, MaxTypesLimitsOutput, InvalidAssembly_ThrowsError.

ExportProjectToolTests methods: ExportsProjectToDirectory, CreatesDirectoryIfNotExists, FailsOnNonEmptyDirectory, ReturnsFileListingWithRelativePaths, InvalidAssembly_ThrowsError.
</phase_7_test_inventory>

<validation_strategy>
Per `.planning/phases/08-tech-debt-cleanup/08-VALIDATION.md`:
- This plan IS the phase-gate validation — the full `dotnet test ILSpy.Mcp.sln` run is the test
- Expected runtime: ~60s full suite on this project size
- Failure policy:
  1. Phase 7 test failures (DecompileNamespaceToolTests, ExportProjectToolTests) → fix in this phase. These are the tests DEBT-04 exists to verify.
  2. Non-Phase-7 failures caused by DEBT-01/02 changes → fix in this phase (expected cleanup).
  3. Unrelated pre-existing failures → defer as new tech-debt item, record in phase summary, mark DEBT-04 Phase 7 verification as passing anyway (because the Phase 7 tests specifically passed).

Dependency on Plan 01: This plan depends on 08-01 because the full-suite run MUST include the DEBT-01/02 changes. If the executor runs the suite before Plan 01 is committed, the run does not validate those changes and the plan's purpose is defeated. Plan 02 (DEBT-03) is a doc-only plan with zero code churn and therefore does not affect dotnet test outcomes — no ordering dependency on Plan 02.
</validation_strategy>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full dotnet test suite and capture the pass/fail summary</name>
  <files>(no file modifications — this task captures a command output as plain text for Task 2 to consume)</files>
  <action>
From the repo root (`C:/Users/cervo/Desktop/Code/ILSpy-MCP` or wherever `ILSpy.Mcp.sln` lives), run:

```bash
dotnet test ILSpy.Mcp.sln
```

Do NOT use a filter. The full suite is required per CONTEXT.md failure policy — a filtered run gives weaker evidence and would not catch regressions elsewhere.

Expected behavior:
- Restore + build + test runs in ~60s on this project
- Exit code `0` indicates all tests passed
- The final output line is a summary in the form `Total tests: N, Passed: P, Failed: F, Skipped: S`

Capture these values from the output (you will need them verbatim in Task 2):
- **Total** test count (expected ~149 after Plan 01 added the new `FindDependencies_NonExistentMember_ThrowsMemberNotFound` test; the pre-Plan-01 baseline was ~148 unconfirmed per the v1.0 audit)
- **Passed** count (must equal Total for a green run)
- **Failed** count (must be 0)
- **Skipped** count (expected 0; any non-zero value should be noted as a deviation)
- **Phase 7 test class presence**: confirm that both `DecompileNamespaceToolTests` and `ExportProjectToolTests` class names appear in the detailed output (verifies the Phase 7 tests actually ran, rather than being silently filtered or skipped). The easiest way is to look at the per-test output lines; most test runners print class.method names.

**If the run fails:**

Apply the failure policy from VALIDATION.md §Failure handling:
1. If a Phase 7 test fails (DecompileNamespaceToolTests.* or ExportProjectToolTests.*) — fix in this phase. These are exactly what DEBT-04 exists to verify.
2. If a non-Phase-7 test fails AND the failure is on the DEBT-01 or DEBT-02 fix path (e.g., asserts on `METHOD_NOT_FOUND` for FindDependenciesTool, or on McpToolException thrown from the use case layer) — fix in this phase (expected cleanup caused by Plan 01 changes).
3. If a non-Phase-7 test fails for an unrelated reason — defer as a new tech-debt item, record in `.planning/STATE.md`, and proceed with DEBT-04 marking the Phase 7 verification as PASSED so long as Phase 7 tests themselves passed.

**If Plan 01 has not yet been merged to the working tree:**

Abort this task. Plan 03 depends on Plan 01 (declared in `depends_on: ["08-01"]` above); the full-suite run must happen on a tree that includes the DEBT-01 and DEBT-02 changes. If the executor sees that 08-01-SUMMARY.md does not exist or that the source files are not in their post-Plan-01 state, stop and run Plan 01 first.

Sanity-check before running:
- `grep -q "MEMBER_NOT_FOUND" Transport/Mcp/Tools/FindDependenciesTool.cs` must succeed (returns exit 0)
- `! grep -q "using ILSpy.Mcp.Transport" Application/UseCases/ExportProjectUseCase.cs` must succeed (layer clean)
- `test -f Domain/Errors/OutputDirectoryNotEmptyException.cs` must succeed (new domain exception exists)

If any of these fail, Plan 01 is not complete and this task must wait.

Record the captured pass/fail counts in plain text (e.g., a scratch variable or local comment) for Task 2 to use when writing the Runtime Verification block.
  </action>
  <verify>
    <automated>dotnet test ILSpy.Mcp.sln</automated>
  </verify>
  <done>
`dotnet test ILSpy.Mcp.sln` returns exit 0. The summary line shows zero failed tests. The pass count is captured in scratch text for Task 2. `DecompileNamespaceToolTests` and `ExportProjectToolTests` both appear as class names in the detailed test output (verifies the Phase 7 classes actually ran).
  </done>
</task>

<task type="auto">
  <name>Task 2: Append Runtime Verification block to three Phase 7 SUMMARY files</name>
  <files>.planning/milestones/v1.0-phases/07-bulk-operations-documentation/07-01-SUMMARY.md, .planning/milestones/v1.0-phases/07-bulk-operations-documentation/07-02-SUMMARY.md, .planning/milestones/v1.0-phases/07-bulk-operations-documentation/07-03-SUMMARY.md</files>
  <action>
Append a `## Runtime Verification` markdown section to each of the three Phase 7 SUMMARY files. Placement: in the markdown BODY (NOT inside the YAML frontmatter), immediately after the existing `## Self-Check: PASSED` block, and BEFORE any trailing document footer. If the existing file has a trailing `---` separator line as a visual footer, place the new block before it; if it ends with plain text, append at the end of the file with a blank line separator.

**CRITICAL: The block goes in the markdown body, NEVER inside the `---` YAML frontmatter at the top of the file.** See `.planning/phases/08-tech-debt-cleanup/08-RESEARCH.md` Pitfall 5 — injecting markdown into a YAML frontmatter region breaks any tool that parses the frontmatter.

**Values to substitute** (use the numbers captured by Task 1):
- `{DATE}` → `2026-04-09` (today's date per the env reminder; use the actual date the run was performed if different)
- `{PASSED}` → the actual passed count from the test summary (e.g., `149`)
- `{FAILED}` → the actual failed count (must be `0` for a green run)
- `{SKIPPED}` → the actual skipped count (expected `0`)

---

**File 1 — `.planning/milestones/v1.0-phases/07-bulk-operations-documentation/07-01-SUMMARY.md`**

Append this block to the body (after `## Self-Check: PASSED`):

```markdown
## Runtime Verification

- **Verified:** {DATE} (Phase 8 DEBT-04)
- **Command:** `dotnet test ILSpy.Mcp.sln`
- **Result:** {PASSED} passed, {FAILED} failed, {SKIPPED} skipped
- **Phase 7 tests in run:** DecompileNamespaceToolTests (6 tests)
- **Closes:** Phase 7 self-check "dotnet CLI not available in execution environment" caveat
```

---

**File 2 — `.planning/milestones/v1.0-phases/07-bulk-operations-documentation/07-02-SUMMARY.md`**

Append this block to the body (after `## Self-Check: PASSED`):

```markdown
## Runtime Verification

- **Verified:** {DATE} (Phase 8 DEBT-04)
- **Command:** `dotnet test ILSpy.Mcp.sln`
- **Result:** {PASSED} passed, {FAILED} failed, {SKIPPED} skipped
- **Phase 7 tests in run:** ExportProjectToolTests (5 tests)
- **Closes:** Phase 7 self-check "dotnet CLI not available in execution environment" caveat
```

---

**File 3 — `.planning/milestones/v1.0-phases/07-bulk-operations-documentation/07-03-SUMMARY.md`**

Append this block to the body. 07-03 was documentation-only (README.md rewrite) and had no tests, so the block is symmetric-for-completeness and says so explicitly:

```markdown
## Runtime Verification

- **Verified:** {DATE} (Phase 8 DEBT-04)
- **Applicable tests:** None — this plan was documentation-only (README.md rewrite)
- **Closes:** N/A (07-03 had no runtime claim to verify). Included for symmetry with 07-01 and 07-02.
```

---

**Sanity check after all three files are updated:**

```bash
grep -l "^## Runtime Verification" .planning/milestones/v1.0-phases/07-bulk-operations-documentation/*-SUMMARY.md
```

Must list all three files: `07-01-SUMMARY.md`, `07-02-SUMMARY.md`, `07-03-SUMMARY.md`.

**Verify no YAML frontmatter was corrupted** (the block is in the body, not the frontmatter):

```bash
for f in .planning/milestones/v1.0-phases/07-bulk-operations-documentation/07-0{1,2,3}-SUMMARY.md; do
  # The first `---` at line 1 and the second `---` (closing the frontmatter) must both exist
  # and the Runtime Verification block must appear AFTER the closing `---` line
  awk '/^---$/{c++} c==2 && /^## Runtime Verification$/{found=1; exit} END{exit !found}' "$f" && echo "OK: $f" || echo "FAIL: $f"
done
```

Must print `OK:` for all three files.

Do NOT:
- Insert the block inside the `---...---` YAML frontmatter at the top of any file (breaks frontmatter parsing)
- Rewrite any existing `## Self-Check: PASSED` content (additive only)
- Touch any other SUMMARY files in Phase 7 or elsewhere
- Edit any source code in this task
  </action>
  <verify>
    <automated>bash -c 'for f in .planning/milestones/v1.0-phases/07-bulk-operations-documentation/07-0{1,2,3}-SUMMARY.md; do grep -q "^## Runtime Verification" "$f" || { echo "MISSING: $f"; exit 1; }; done; echo OK'</automated>
  </verify>
  <done>
All three Phase 7 SUMMARY files contain a `## Runtime Verification` markdown section in the body (not the frontmatter). The sections record date, command (`dotnet test ILSpy.Mcp.sln`), actual pass/fail counts from Task 1, and Phase 7 test class names. The AWK sanity check confirms the blocks are placed AFTER the closing frontmatter delimiter, not inside it. Grep confirms all three files have the new heading.
  </done>
</task>

</tasks>

<verification>
Phase check after both tasks complete (corresponds to ROADMAP.md Phase 8 success criterion #4):

```bash
# 1. Full suite green (Task 1)
dotnet test ILSpy.Mcp.sln
# Exit code 0, zero failures.

# 2. Runtime Verification blocks present (Task 2)
grep -l "^## Runtime Verification" .planning/milestones/v1.0-phases/07-bulk-operations-documentation/*-SUMMARY.md | wc -l
# Must print 3.

# 3. Frontmatter integrity — Runtime Verification block is in body, not YAML
for f in .planning/milestones/v1.0-phases/07-bulk-operations-documentation/07-0{1,2,3}-SUMMARY.md; do
  awk '/^---$/{c++} c==2 && /^## Runtime Verification$/{found=1; exit} END{exit !found}' "$f" && echo "OK: $f" || echo "FAIL: $f"
done
# All three must print OK.

# 4. Phase 7 test classes actually ran (spot-check via filter)
dotnet test ILSpy.Mcp.sln --filter "FullyQualifiedName~DecompileNamespaceToolTests|FullyQualifiedName~ExportProjectToolTests"
# Must report 11 tests (6 + 5), all passing.
```
</verification>

<success_criteria>
- [ ] `dotnet test ILSpy.Mcp.sln` runs green (exit code 0, zero failures)
- [ ] Phase 7 test classes `DecompileNamespaceToolTests` and `ExportProjectToolTests` both appear in the test run output (verifies they actually executed)
- [ ] All three Phase 7 SUMMARY files contain a `## Runtime Verification` markdown section in the body
- [ ] Each block includes: date, command (`dotnet test ILSpy.Mcp.sln`), pass/fail/skipped counts, and identification of the Phase 7 tests in scope for that file
- [ ] The Runtime Verification block is AFTER the closing `---` of the frontmatter, NOT inside it (AWK check passes)
- [ ] `grep -c "^## Runtime Verification" .planning/milestones/v1.0-phases/07-bulk-operations-documentation/*-SUMMARY.md` returns `1` for each of the three files
- [ ] The existing `## Self-Check: PASSED` blocks are preserved unchanged (additive only)
- [ ] Phase 7's "verified by code inspection only" hedge is now closed by a captured runtime artifact
</success_criteria>

<output>
After completion, create `.planning/phases/08-tech-debt-cleanup/08-03-SUMMARY.md` with:

- `requirements-completed: [DEBT-04]` in frontmatter
- Accomplishments: record the actual `dotnet test` summary line (Total, Passed, Failed, Skipped) — this IS the evidence artifact for the phase
- Record any test-count delta vs the pre-Phase-8 baseline (pre: ~148 unconfirmed; post: ~149 with the new FindDependencies_NonExistentMember_ThrowsMemberNotFound test from Plan 01)
- List the three Phase 7 SUMMARY files that received the Runtime Verification block
- Deviation recorded: if any tests were skipped or if any unrelated failures were deferred, note them here as new tech-debt candidates for a future phase
- Note: Phase 8 is now fully complete. All four DEBT requirements (DEBT-01, DEBT-02, DEBT-03, DEBT-04) are closed across three plans and two waves.
</output>
