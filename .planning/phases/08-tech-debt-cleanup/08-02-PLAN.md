---
phase: 08-tech-debt-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v1.0-phases/01-test-infrastructure-baseline/01-01-SUMMARY.md
  - .planning/milestones/v1.0-phases/01-test-infrastructure-baseline/01-02-SUMMARY.md
  - .planning/milestones/v1.0-phases/02-sdk-upgrades-bug-fixes/02-01-SUMMARY.md
  - .planning/milestones/v1.0-phases/02-sdk-upgrades-bug-fixes/02-02-SUMMARY.md
  - .planning/milestones/v1.0-phases/02-sdk-upgrades-bug-fixes/02-03-SUMMARY.md
  - .planning/milestones/v1.0-phases/06-search-cross-assembly/06-01-SUMMARY.md
autonomous: true
requirements:
  - DEBT-03

must_haves:
  truths:
    - "Every Phase 1-6 SUMMARY.md file has a requirements-completed frontmatter key"
    - "The six backfilled files are exactly: 01-01, 01-02, 02-01, 02-02, 02-03, 06-01 (no others are touched)"
    - "Existing frontmatter shapes (Phase 1/2 dependency_graph nested shape, Phase 3+ flat shape) are preserved untouched — only the single new key is added"
    - "After backfill, the count of SUMMARY files containing requirements-completed key is 16 (10 pre-existing + 6 newly backfilled)"
  artifacts:
    - path: ".planning/milestones/v1.0-phases/01-test-infrastructure-baseline/01-01-SUMMARY.md"
      provides: "Frontmatter with requirements-completed: [] (infrastructure-only plan)"
      contains: "requirements-completed:"
    - path: ".planning/milestones/v1.0-phases/01-test-infrastructure-baseline/01-02-SUMMARY.md"
      provides: "Frontmatter with requirements-completed: [TEST-01]"
      contains: "requirements-completed:"
    - path: ".planning/milestones/v1.0-phases/02-sdk-upgrades-bug-fixes/02-01-SUMMARY.md"
      provides: "Frontmatter with requirements-completed: [SDK-01, SDK-02]"
      contains: "requirements-completed:"
    - path: ".planning/milestones/v1.0-phases/02-sdk-upgrades-bug-fixes/02-02-SUMMARY.md"
      provides: "Frontmatter with requirements-completed: [SDK-03, SDK-04] (added alongside existing requirements: key)"
      contains: "requirements-completed:"
    - path: ".planning/milestones/v1.0-phases/02-sdk-upgrades-bug-fixes/02-03-SUMMARY.md"
      provides: "Frontmatter with requirements-completed: [SDK-05, TEST-04] (added alongside existing requirements: key)"
      contains: "requirements-completed:"
    - path: ".planning/milestones/v1.0-phases/06-search-cross-assembly/06-01-SUMMARY.md"
      provides: "Frontmatter with requirements-completed: [SRCH-01, SRCH-02]"
      contains: "requirements-completed:"
  key_links: []
---

<objective>
Close DEBT-03 by adding the `requirements-completed:` frontmatter key to the six Phase 1-6 SUMMARY.md files that actually lack it. This is a doc-only, additive change — no source code touched, no tests run.

Purpose: Give the audit tooling a uniform requirement→plan mapping for every historical v1.0 plan, closing the Phase 1-6 frontmatter gap that the v1.0 milestone audit flagged.

Output: Six .planning/milestones/v1.0-phases/* SUMMARY.md files with one new frontmatter key each. No other lines in those files change.

**IMPORTANT**: CONTEXT.md lists 13 files and mentions 14 — both are wrong. RESEARCH.md verified by ripgrep that only 6 files actually lack the key. Trust the 6-file list below. The other 7 files (`03-01`, `03-02`, `04-01`, `04-02`, `05-01`, `05-02`, `06-02`) already have the key and MUST NOT be touched (no-churn policy).
</objective>

<execution_context>
@C:/Users/cervo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/cervo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-tech-debt-cleanup/08-CONTEXT.md
@.planning/phases/08-tech-debt-cleanup/08-RESEARCH.md
@.planning/phases/08-tech-debt-cleanup/08-VALIDATION.md

# The six files this plan edits
@.planning/milestones/v1.0-phases/01-test-infrastructure-baseline/01-01-SUMMARY.md
@.planning/milestones/v1.0-phases/01-test-infrastructure-baseline/01-02-SUMMARY.md
@.planning/milestones/v1.0-phases/02-sdk-upgrades-bug-fixes/02-01-SUMMARY.md
@.planning/milestones/v1.0-phases/02-sdk-upgrades-bug-fixes/02-02-SUMMARY.md
@.planning/milestones/v1.0-phases/02-sdk-upgrades-bug-fixes/02-03-SUMMARY.md
@.planning/milestones/v1.0-phases/06-search-cross-assembly/06-01-SUMMARY.md

# Reference: the Phase 7 shape precedent (already has the key — do NOT edit)
@.planning/milestones/v1.0-phases/07-bulk-operations-documentation/07-01-SUMMARY.md

<interfaces>
Frontmatter placement rule:
- Phase 1/2 files use the `dependency_graph:` nested shape with a trailing `metrics:` block. Insert `requirements-completed: [...]` on a single new line, immediately BEFORE the `metrics:` key.
- Phase 6 file (`06-01-SUMMARY.md`) also uses the nested/underscore shape (per RESEARCH.md). Same rule: before `metrics:`.
- Phase 7 precedent (do not imitate, just reference): `requirements-completed:` sits between `patterns-established:` and `duration:` on the flat shape.

YAML legality: kebab-case and snake_case keys can coexist in the same YAML document. No tool in this repo enforces a strict schema. Adding one kebab-case key next to `tech_stack`/`key_files` is safe.

Do-not-touch list (already have the key, verified 2026-04-09):
- 03-01, 03-02 (Phase 3)
- 04-01, 04-02 (Phase 4)
- 05-01, 05-02 (Phase 5)
- 06-02 (Phase 6)
- 07-01, 07-02, 07-03 (Phase 7)

Per-file mapping (authoritative — from RESEARCH.md §DEBT-03):

| File | Value to set | Note |
|------|--------------|------|
| 01-01-SUMMARY.md | `[]` | Infrastructure-only plan (TestTargets + ToolTestFixture); TEST-01 is credited to 01-02 which built the actual regression tests. |
| 01-02-SUMMARY.md | `[TEST-01]` | 31 regression tests for all 8 tools |
| 02-01-SUMMARY.md | `[SDK-01, SDK-02]` | SDK + decompiler upgrade, landed in one commit |
| 02-02-SUMMARY.md | `[SDK-03, SDK-04]` | ConcurrencyLimiter + TimeoutToken disposable; NOTE: file already has a `requirements:` key at line 41 with these same values — ADD `requirements-completed:` alongside it, do NOT rename. |
| 02-03-SUMMARY.md | `[SDK-05, TEST-04]` | Bug fixes + test expansion; same pattern — file has existing `requirements:` key at line 31, ADD alongside. |
| 06-01-SUMMARY.md | `[SRCH-01, SRCH-02]` | search_strings and search_constants tools |
</interfaces>

<validation_strategy>
Per `.planning/phases/08-tech-debt-cleanup/08-VALIDATION.md`:
- No test runner involvement — this is a doc-only plan
- Verification is a single grep check confirming the key is present in all 6 files
- The failure mode is "MISSING: {file}" output from the verification loop

Parallel safety: This plan operates exclusively on `.planning/milestones/v1.0-phases/*/*.md` files. Plan 01 operates on source code under `Domain/`, `Application/`, `Transport/`, `Tests/`. Zero file-path overlap → Plans 01 and 02 can run in parallel in Wave 1.
</validation_strategy>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Backfill requirements-completed frontmatter key in 6 Phase 1-6 SUMMARY files</name>
  <files>.planning/milestones/v1.0-phases/01-test-infrastructure-baseline/01-01-SUMMARY.md, .planning/milestones/v1.0-phases/01-test-infrastructure-baseline/01-02-SUMMARY.md, .planning/milestones/v1.0-phases/02-sdk-upgrades-bug-fixes/02-01-SUMMARY.md, .planning/milestones/v1.0-phases/02-sdk-upgrades-bug-fixes/02-02-SUMMARY.md, .planning/milestones/v1.0-phases/02-sdk-upgrades-bug-fixes/02-03-SUMMARY.md, .planning/milestones/v1.0-phases/06-search-cross-assembly/06-01-SUMMARY.md</files>
  <action>
Add a single line `requirements-completed: [<values>]` to the YAML frontmatter of each of the six files listed below. Placement: immediately before the `metrics:` key in each file. No other modifications.

**File 1 — `.planning/milestones/v1.0-phases/01-test-infrastructure-baseline/01-01-SUMMARY.md`**

Find the `metrics:` key in the frontmatter. Immediately before it, insert this new line:

```yaml
requirements-completed: []
```

The empty array reflects an ambiguity decision: TEST-01 ("All existing tools have regression tests") is SATISFIED by 01-02's 31 regression tests, not by 01-01 which built the TestTargets + ToolTestFixture infrastructure foundation. 01-01 is infrastructure-only and gets no credit for TEST-01. This choice is noted as a deviation in the phase execution summary per CONTEXT.md ambiguity policy.

**File 2 — `.planning/milestones/v1.0-phases/01-test-infrastructure-baseline/01-02-SUMMARY.md`**

Find the `metrics:` key. Immediately before it, insert:

```yaml
requirements-completed: [TEST-01]
```

01-02 built the 31-test regression suite that actually satisfies TEST-01.

**File 3 — `.planning/milestones/v1.0-phases/02-sdk-upgrades-bug-fixes/02-01-SUMMARY.md`**

Find the `metrics:` key. Immediately before it, insert:

```yaml
requirements-completed: [SDK-01, SDK-02]
```

SDK-01 is the MCP SDK upgrade (0.4.0-preview.3 → 1.2.0) and SDK-02 is the decompiler upgrade (9.1.0 → 10.0.0). Both landed in the same commit per the summary body.

**File 4 — `.planning/milestones/v1.0-phases/02-sdk-upgrades-bug-fixes/02-02-SUMMARY.md`**

NOTE: This file ALREADY has a `requirements:` key (singular name) at line 41 with value `[SDK-03, SDK-04]`. Do NOT rename or delete it. Find the `metrics:` key at line 42 (approximately) and immediately before it, insert:

```yaml
requirements-completed: [SDK-03, SDK-04]
```

After the edit, the file has BOTH `requirements:` (pre-existing, left alone) AND `requirements-completed:` (newly added). This is per CONTEXT.md "do not churn" policy — additive-only.

**File 5 — `.planning/milestones/v1.0-phases/02-sdk-upgrades-bug-fixes/02-03-SUMMARY.md`**

Same pattern as File 4: the existing `requirements:` key at line 31 carries `[SDK-05, TEST-04]`. Do NOT touch it. Find the `metrics:` key and immediately before it, insert:

```yaml
requirements-completed: [SDK-05, TEST-04]
```

**File 6 — `.planning/milestones/v1.0-phases/06-search-cross-assembly/06-01-SUMMARY.md`**

Find the `metrics:` key. Immediately before it, insert:

```yaml
requirements-completed: [SRCH-01, SRCH-02]
```

SRCH-01 is `search_strings` and SRCH-02 is `search_constants`.

---

**After all six edits, run the verification grep loop from the repo root:**

```bash
for f in 01-test-infrastructure-baseline/01-01 01-test-infrastructure-baseline/01-02 02-sdk-upgrades-bug-fixes/02-01 02-sdk-upgrades-bug-fixes/02-02 02-sdk-upgrades-bug-fixes/02-03 06-search-cross-assembly/06-01; do
  grep -q "^requirements-completed:" ".planning/milestones/v1.0-phases/${f}-SUMMARY.md" || echo "MISSING: ${f}"
done
```

Expected output: empty (no MISSING lines). Any `MISSING:` output indicates a file was not updated.

**Sanity check — ensure no churn on the 7 files that already had the key:**

```bash
git diff --stat .planning/milestones/v1.0-phases/03-il-infrastructure-disassembly/ .planning/milestones/v1.0-phases/04-cross-reference-analysis/ .planning/milestones/v1.0-phases/05-assembly-inspection/ .planning/milestones/v1.0-phases/06-search-cross-assembly/06-02-SUMMARY.md .planning/milestones/v1.0-phases/07-bulk-operations-documentation/
```

Expected output: empty (none of these should have been touched). If any file there shows a diff, that is a scope violation — revert.

**Total git diff expected** (approximately): 6 files changed, +6 insertions, 0 deletions (one line added per file).

Do NOT:
- Reshape the `dependency_graph:` nested structure into `requires:`/`provides:`/`affects:` flat shape — explicitly deferred per CONTEXT.md
- Rename `tech_stack:` → `tech-stack:`, `key_files:` → `key-files:`, etc. — explicitly deferred
- Delete or rename the existing `requirements:` key on 02-02 or 02-03 — additive only
- Touch ANY of the 7 already-have-the-key files (03-01, 03-02, 04-01, 04-02, 05-01, 05-02, 06-02) — no churn
- Touch Phase 7 SUMMARY files (07-01, 07-02, 07-03) — they are untouched by DEBT-03 and will be edited separately by Plan 03 (DEBT-04) with the Runtime Verification block
  </action>
  <verify>
    <automated>bash -c 'for f in 01-test-infrastructure-baseline/01-01 01-test-infrastructure-baseline/01-02 02-sdk-upgrades-bug-fixes/02-01 02-sdk-upgrades-bug-fixes/02-02 02-sdk-upgrades-bug-fixes/02-03 06-search-cross-assembly/06-01; do grep -q "^requirements-completed:" ".planning/milestones/v1.0-phases/${f}-SUMMARY.md" || { echo "MISSING: ${f}"; exit 1; }; done; echo OK'</automated>
  </verify>
  <done>
The verification loop prints `OK` (zero MISSING lines). `git diff --stat` for the six files shows exactly one line added per file (total: 6 insertions, 0 deletions). The seven files in the do-not-touch list show zero diff. `grep -rc "^requirements-completed:" .planning/milestones/v1.0-phases/` now returns 16 total matches (10 pre-existing + 6 newly backfilled).
  </done>
</task>

</tasks>

<verification>
Phase check after task completes (corresponds to ROADMAP.md Phase 8 success criterion #3):

```bash
grep -c "^requirements-completed:" .planning/milestones/v1.0-phases/*/*-SUMMARY.md | grep -v ":0$"
```

Every SUMMARY.md file in Phases 1-7 should appear in the output with a count of 1. Any file with `:0` indicates it is still missing the key.

Alternative single-line check:

```bash
find .planning/milestones/v1.0-phases -name "*-SUMMARY.md" | while read f; do grep -q "^requirements-completed:" "$f" || echo "MISSING: $f"; done
```

Expected output: empty.
</verification>

<success_criteria>
- [ ] All six target files have `requirements-completed:` frontmatter key
- [ ] Values per file match the mapping: 01-01 `[]`, 01-02 `[TEST-01]`, 02-01 `[SDK-01, SDK-02]`, 02-02 `[SDK-03, SDK-04]`, 02-03 `[SDK-05, TEST-04]`, 06-01 `[SRCH-01, SRCH-02]`
- [ ] Existing `requirements:` keys on 02-02 and 02-03 are preserved alongside the new keys
- [ ] Zero files outside the six-file list are modified (verified by `git diff --stat`)
- [ ] Total diff is approximately +6 lines, 0 deletions across 6 files
- [ ] After this plan, `grep -rc "^requirements-completed:" .planning/milestones/v1.0-phases/` counts 16 total matches across all SUMMARY files
</success_criteria>

<output>
After completion, create `.planning/phases/08-tech-debt-cleanup/08-02-SUMMARY.md` with:

- `requirements-completed: [DEBT-03]` in frontmatter (eating our own dog food — the new Phase 8 summary itself carries the key)
- Accomplishments: list the six files touched and the per-file requirement values
- Deviation recorded: `01-01-SUMMARY.md` was given `[]` rather than `[TEST-01]` because the TEST-01 requirement is satisfied by 01-02's regression tests, not by 01-01's infrastructure. This matches the CONTEXT.md ambiguity policy interpreted as "credit the plan that produced the user-observable effect."
- Note: no test runner involvement; verification was grep-only; Plan 01 and Plan 02 ran in parallel in Wave 1
</output>
