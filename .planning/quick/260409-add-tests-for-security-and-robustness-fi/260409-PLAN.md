---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Tests/Security/ErrorSanitizerTests.cs
  - Tests/Security/ILSpyOptionsValidatorTests.cs
  - Tests/Security/SecurityAndRobustnessTests.cs
autonomous: true
must_haves:
  truths:
    - "ReDoS pattern does not hang — completes or errors within seconds"
    - "Negative offset/limit on resource extraction returns error"
    - "Export with null/empty directory throws"
    - "Decompilation truncation works with small MaxDecompilationSize"
    - "ILSpyOptions rejects invalid config values"
    - "ErrorSanitizer strips directory paths from messages"
  artifacts:
    - path: "Tests/Security/ErrorSanitizerTests.cs"
      provides: "Unit tests for ErrorSanitizer.SanitizePath"
    - path: "Tests/Security/ILSpyOptionsValidatorTests.cs"
      provides: "Unit tests for ILSpyOptions.Validate and ILSpyOptionsValidator"
    - path: "Tests/Security/SecurityAndRobustnessTests.cs"
      provides: "Integration tests for ReDoS, negative offset/limit, export path, truncation"
  key_links: []
---

<objective>
Add tests verifying security and robustness fixes applied to the ILSpy-MCP project.

Purpose: Ensure ReDoS prevention, input validation, output truncation, path sanitization, and options validation all work correctly and don't regress.
Output: Three test files covering all seven fix categories.
</objective>

<execution_context>
@C:/Users/cervo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/cervo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Tests/Fixtures/ToolTestFixture.cs
@Transport/Mcp/Errors/ErrorSanitizer.cs
@Application/Configuration/ILSpyOptions.cs
@Tests/Tools/SearchStringsToolTests.cs (pattern reference)
@Tests/Tools/ExtractResourceToolTests.cs (pattern reference)
@Tests/Tools/ExportProjectToolTests.cs (pattern reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit tests for ErrorSanitizer and ILSpyOptionsValidator</name>
  <files>Tests/Security/ErrorSanitizerTests.cs, Tests/Security/ILSpyOptionsValidatorTests.cs</files>
  <action>
Create `Tests/Security/ErrorSanitizerTests.cs` — pure unit tests, no fixture needed:
- Test Windows path: `SanitizePath("Failed to load C:\\Users\\admin\\secrets\\myapp.dll")` should return `"Failed to load myapp.dll"` (directory stripped, filename kept)
- Test Unix path: `SanitizePath("Error at /opt/app/bin/assembly.dll")` strips to `"Error at assembly.dll"`
- Test message with no paths: `SanitizePath("Simple error message")` returns unchanged
- Test multiple paths in one message: both get sanitized
- Test path with spaces in quotes if relevant

Create `Tests/Security/ILSpyOptionsValidatorTests.cs` — pure unit tests:
- `MaxDecompilationSize = 0` causes Validate() to throw `ArgumentOutOfRangeException`
- `MaxDecompilationSize = -1` causes Validate() to throw
- `MaxDecompilationSize = 500_000_001` (over 500MB) causes Validate() to throw
- `DefaultTimeoutSeconds = 0` causes Validate() to throw
- `DefaultTimeoutSeconds = 3601` causes Validate() to throw
- `MaxConcurrentOperations = 0` causes Validate() to throw
- `MaxConcurrentOperations = 101` causes Validate() to throw
- Valid defaults pass Validate() without throwing
- Test via `ILSpyOptionsValidator.Validate()` method: invalid config returns `ValidateOptionsResult` with failure

Both files: use `namespace ILSpy.Mcp.Tests.Security`, use xUnit `[Fact]`, FluentAssertions. No `[Collection]` needed since these are pure unit tests with no shared fixture.
  </action>
  <verify>
    <automated>dotnet test Tests/ --filter "FullyQualifiedName~Security.ErrorSanitizer|FullyQualifiedName~Security.ILSpyOptionsValidator" --no-build 2>/dev/null || dotnet test Tests/ --filter "FullyQualifiedName~Security.ErrorSanitizer|FullyQualifiedName~Security.ILSpyOptionsValidator"</automated>
  </verify>
  <done>All ErrorSanitizer and ILSpyOptionsValidator tests pass. Invalid options are rejected, valid options pass, paths are stripped from error messages.</done>
</task>

<task type="auto">
  <name>Task 2: Integration tests for security/robustness fixes through tool layer</name>
  <files>Tests/Security/SecurityAndRobustnessTests.cs</files>
  <action>
Create `Tests/Security/SecurityAndRobustnessTests.cs` with `[Collection("ToolTests")]` and `ToolTestFixture` injection.

**ReDoS prevention test:**
- Resolve `SearchStringsTool` from scope
- Call `ExecuteAsync` with pattern `(a+)+b` against `_fixture.TestAssemblyPath`
- Use a 10-second `CancellationTokenSource` as safety net
- The call should either complete quickly (regex timeout fires at 1s) or throw `McpToolException` with `INVALID_PATTERN` — assert it does NOT hang. Accept either outcome (error or fast completion).

**Negative offset/limit on ExtractResource:**
- Resolve `ExtractResourceTool`
- Call with `offset: -1` on a valid resource name `"ILSpy.Mcp.TestTargets.Resources.sample.txt"` — should throw `McpToolException`
- Call with `limit: 0` — should throw `McpToolException`
- Call with `limit: -5` — should throw `McpToolException`

**Export path validation:**
- Resolve `ExportProjectTool`
- Call with `outputDirectory: ""` — should throw `McpToolException`
- Call with `outputDirectory: "   "` (whitespace) — should throw `McpToolException`

**MaxDecompilationSize truncation:**
- Build a custom `ServiceProvider` (small helper method) with `MaxDecompilationSize = 100` (100 bytes) — register same services as `ToolTestFixture` but override the options
- Resolve `DecompileTypeTool` from custom provider
- Call `ExecuteAsync` for `"ILSpy.Mcp.TestTargets.SimpleClass"` which will produce output > 100 bytes
- Assert result contains `"[Output truncated at 100 bytes"` message
- Dispose custom provider after test

For the custom provider helper: duplicate the `ToolTestFixture` service registrations in a private static method, but with `MaxDecompilationSize = 100`. Keep it minimal — just what DecompileTypeTool needs (IDecompilerService, DecompileTypeUseCase, ITimeoutService, IConcurrencyLimiter, IOptions of ILSpyOptions, logging).
  </action>
  <verify>
    <automated>dotnet test Tests/ --filter "FullyQualifiedName~Security.SecurityAndRobustness" --no-build 2>/dev/null || dotnet test Tests/ --filter "FullyQualifiedName~Security.SecurityAndRobustness"</automated>
  </verify>
  <done>All integration tests pass: ReDoS pattern completes within seconds (no hang), negative offset/limit rejected, empty export path rejected, truncation message appears with small MaxDecompilationSize.</done>
</task>

</tasks>

<verification>
Run all new tests together:
```
dotnet test Tests/ --filter "FullyQualifiedName~Security" -v normal
```
All tests green. No existing tests broken:
```
dotnet test Tests/ -v minimal
```
</verification>

<success_criteria>
- All 7 fix categories have at least one test covering them
- No test takes more than 10 seconds (ReDoS test in particular)
- All existing tests continue to pass
- Tests follow project conventions (xUnit, FluentAssertions, tool-level integration pattern)
</success_criteria>

<output>
After completion, create `.planning/quick/260409-add-tests-for-security-and-robustness-fi/260409-SUMMARY.md`
</output>
